<%= form_for(@gen_artifact_nail_head) do |f| %>
  <% if @gen_artifact_nail_head.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gen_artifact_nail_head.errors.count, "error") %> prohibited this gen_artifact_nail_head from being saved:</h2>

      <ul>
      <% @gen_artifact_nail_head.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nail_head_type %><br />
    <%= f.text_field :nail_head_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
