<%= form_for(@context_sample_type) do |f| %>
  <% if @context_sample_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@context_sample_type.errors.count, "error") %> prohibited this context_sample_type from being saved:</h2>

      <ul>
      <% @context_sample_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sample_type %><br />
    <%= f.text_field :sample_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
