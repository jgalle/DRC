<%= form_for(@buckle) do |f| %>
  <% if @buckle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buckle.errors.count, "error") %> prohibited this buckle from being saved:</h2>

      <ul>
      <% @buckle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :article_id %><br />
    <%= f.text_field :article_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
