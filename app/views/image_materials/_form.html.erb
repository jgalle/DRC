<%= form_for(@image_material) do |f| %>
  <% if @image_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_material.errors.count, "error") %> prohibited this image_material from being saved:</h2>

      <ul>
      <% @image_material.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ImageMaterial %><br>
    <%= f.text_field :ImageMaterial %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
