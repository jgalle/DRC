<%= form_for(@basic_color) do |f| %>
  <% if @basic_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@basic_color.errors.count, "error") %> prohibited this basic_color from being saved:</h2>

      <ul>
      <% @basic_color.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :BasicColor %><br />
    <%= f.text_field :BasicColor %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
