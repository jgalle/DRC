<%= form_for(@bead_shape) do |f| %>
  <% if @bead_shape.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bead_shape.errors.count, "error") %> prohibited this bead_shape from being saved:</h2>

      <ul>
      <% @bead_shape.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bead_shape %><br />
    <%= f.text_field :bead_shape %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
