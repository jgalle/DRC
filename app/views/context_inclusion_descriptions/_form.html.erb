<%= form_for(@context_inclusion_description) do |f| %>
  <% if @context_inclusion_description.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@context_inclusion_description.errors.count, "error") %> prohibited this context_inclusion_description from being saved:</h2>

      <ul>
      <% @context_inclusion_description.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :inclusion_description %><br />
    <%= f.text_field :inclusion_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
