<%= form_for(@context_sample_location) do |f| %>
  <% if @context_sample_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@context_sample_location.errors.count, "error") %> prohibited this context_sample_location from being saved:</h2>

      <ul>
      <% @context_sample_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
