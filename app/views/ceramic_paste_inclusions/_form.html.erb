<%= form_for(@ceramic_paste_inclusion) do |f| %>
  <% if @ceramic_paste_inclusion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ceramic_paste_inclusion.errors.count, "error") %> prohibited this ceramic_paste_inclusion from being saved:</h2>

      <ul>
      <% @ceramic_paste_inclusion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artifact_id %><br />
    <%= f.text_field :artifact_id %>
  </div>
  <div class="field">
    <%= f.label :paste_inclusion_id %><br />
    <%= f.text_field :paste_inclusion_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
