$('.date-sort-default').show();
$('.date-sort-ascending').hide();
$('.date-sort-descending').hide();
$('.user-sort-default').hide();
$('.user-sort-ascending').hide();
$('.user-sort-descending').show();
$('.subject-sort-default').show();
$('.subject-sort-ascending').hide();
$('.subject-sort-descending').hide();
if ($('.date-sort-default').attr('data-all') != null) {
	$('#message-list').html('<%= escape_javascript("#{render :partial => 'messages', :locals => {:from_descend => true, :all_messages => true}}").html_safe %>');
	console.log('desc all');
} else {
	$('#message-list').html('<%= escape_javascript("#{render :partial => 'messages', :locals => {:from_descend => true}}").html_safe %>');
	console.log('desc');
}
console.log('sorted');
