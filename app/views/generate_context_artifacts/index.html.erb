<h1>Listing generate_context_artifacts</h1>

<table>
  <tr>
    <th>Context sample</th>
    <th>Artifact</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @generate_context_artifacts.each do |generate_context_artifact| %>
  <tr>
    <td><%= generate_context_artifact.context_sample_id %></td>
    <td><%= generate_context_artifact.artifact_id %></td>
    <td><%= link_to 'Show', generate_context_artifact %></td>
    <td><%= link_to 'Edit', edit_generate_context_artifact_path(generate_context_artifact) %></td>
    <td><%= link_to 'Destroy', generate_context_artifact, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Generate context artifact', new_generate_context_artifact_path %>
