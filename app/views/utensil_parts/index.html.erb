<h1>Listing utensil_parts</h1>

<table>
  <tr>
    <th>Utensil part</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @utensil_parts.each do |utensil_part| %>
  <tr>
    <td><%= utensil_part.utensil_part %></td>
    <td><%= link_to 'Show', utensil_part %></td>
    <td><%= link_to 'Edit', edit_utensil_part_path(utensil_part) %></td>
    <td><%= link_to 'Destroy', utensil_part, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Utensil part', new_utensil_part_path %>
