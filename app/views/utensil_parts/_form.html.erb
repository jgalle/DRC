<%= form_for(@utensil_part) do |f| %>
  <% if @utensil_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@utensil_part.errors.count, "error") %> prohibited this utensil_part from being saved:</h2>

      <ul>
      <% @utensil_part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :utensil_part %><br />
    <%= f.text_field :utensil_part %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
