<%= form_for(@tobacco_pipe_material) do |f| %>
  <% if @tobacco_pipe_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tobacco_pipe_material.errors.count, "error") %> prohibited this tobacco_pipe_material from being saved:</h2>

      <ul>
      <% @tobacco_pipe_material.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pipe_material %><br />
    <%= f.text_field :pipe_material %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
