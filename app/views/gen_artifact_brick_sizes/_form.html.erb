<%= form_for(@gen_artifact_brick_size) do |f| %>
  <% if @gen_artifact_brick_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gen_artifact_brick_size.errors.count, "error") %> prohibited this gen_artifact_brick_size from being saved:</h2>

      <ul>
      <% @gen_artifact_brick_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :brick_size %><br />
    <%= f.text_field :brick_size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
