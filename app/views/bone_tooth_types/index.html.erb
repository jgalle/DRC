<h1>Listing bone_tooth_types</h1>

<table>
  <tr>
    <th>Tooth type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bone_tooth_types.each do |bone_tooth_type| %>
  <tr>
    <td><%= bone_tooth_type.tooth_type %></td>
    <td><%= link_to 'Show', bone_tooth_type %></td>
    <td><%= link_to 'Edit', edit_bone_tooth_type_path(bone_tooth_type) %></td>
    <td><%= link_to 'Destroy', bone_tooth_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bone tooth type', new_bone_tooth_type_path %>
