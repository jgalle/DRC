<%= form_for(@bone_mark_direction) do |f| %>
  <% if @bone_mark_direction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bone_mark_direction.errors.count, "error") %> prohibited this bone_mark_direction from being saved:</h2>

      <ul>
      <% @bone_mark_direction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :mark_direction %><br />
    <%= f.text_field :mark_direction %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
