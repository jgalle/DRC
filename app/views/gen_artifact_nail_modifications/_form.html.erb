<%= form_for(@gen_artifact_nail_modification) do |f| %>
  <% if @gen_artifact_nail_modification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gen_artifact_nail_modification.errors.count, "error") %> prohibited this gen_artifact_nail_modification from being saved:</h2>

      <ul>
      <% @gen_artifact_nail_modification.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nail_modification %><br />
    <%= f.text_field :nail_modification %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
