<%= form_for(@bead_structure) do |f| %>
  <% if @bead_structure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bead_structure.errors.count, "error") %> prohibited this bead_structure from being saved:</h2>

      <ul>
      <% @bead_structure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :structure %><br />
    <%= f.text_field :structure %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
