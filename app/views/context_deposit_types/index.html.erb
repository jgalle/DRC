<h1>Listing context_deposit_types</h1>

<table>
  <tr>
    <th>Deposit type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @context_deposit_types.each do |context_deposit_type| %>
  <tr>
    <td><%= context_deposit_type.deposit_type %></td>
    <td><%= link_to 'Show', context_deposit_type %></td>
    <td><%= link_to 'Edit', edit_context_deposit_type_path(context_deposit_type) %></td>
    <td><%= link_to 'Destroy', context_deposit_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Context deposit type', new_context_deposit_type_path %>
