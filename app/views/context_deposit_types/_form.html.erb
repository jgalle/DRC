<%= form_for(@context_deposit_type) do |f| %>
  <% if @context_deposit_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@context_deposit_type.errors.count, "error") %> prohibited this context_deposit_type from being saved:</h2>

      <ul>
      <% @context_deposit_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :deposit_type %><br />
    <%= f.text_field :deposit_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
