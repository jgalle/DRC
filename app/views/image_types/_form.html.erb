<%= form_for(@image_type) do |f| %>
  <% if @image_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@image_type.errors.count, "error") %> prohibited this image_type from being saved:</h2>

      <ul>
      <% @image_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image_type %><br />
    <%= f.text_field :image_type %>
  </div>
  <div class="field">
    <%= f.label :image_table %><br />
    <%= f.text_field :image_table %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
