<%= form_for(@buckle_type) do |f| %>
  <% if @buckle_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buckle_type.errors.count, "error") %> prohibited this buckle_type from being saved:</h2>

      <ul>
      <% @buckle_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :buckle_type %><br />
    <%= f.text_field :buckle_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
