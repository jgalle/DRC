<%= form_for(@bead_form) do |f| %>
  <% if @bead_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bead_form.errors.count, "error") %> prohibited this bead_form from being saved:</h2>

      <ul>
      <% @bead_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bead_form %><br />
    <%= f.text_field :bead_form %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
