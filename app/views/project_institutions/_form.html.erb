<%= form_for(@project_institution) do |f| %>
  <% if @project_institution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_institution.errors.count, "error") %> prohibited this project_institution from being saved:</h2>

      <ul>
      <% @project_institution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :institution %><br />
    <%= f.text_field :institution %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
