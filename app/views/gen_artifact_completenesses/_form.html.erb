<%= form_for(@gen_artifact_completeness) do |f| %>
  <% if @gen_artifact_completeness.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gen_artifact_completeness.errors.count, "error") %> prohibited this gen_artifact_completeness from being saved:</h2>

      <ul>
      <% @gen_artifact_completeness.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :gen_artifact_completeness %><br />
    <%= f.text_field :gen_artifact_completeness %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
