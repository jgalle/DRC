<%= form_for(@bone_mark_type) do |f| %>
  <% if @bone_mark_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bone_mark_type.errors.count, "error") %> prohibited this bone_mark_type from being saved:</h2>

      <ul>
      <% @bone_mark_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bone_mark_type %><br />
    <%= f.text_field :bone_mark_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
