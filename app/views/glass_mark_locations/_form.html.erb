<%= form_for(@glass_mark_location) do |f| %>
  <% if @glass_mark_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@glass_mark_location.errors.count, "error") %> prohibited this glass_mark_location from being saved:</h2>

      <ul>
      <% @glass_mark_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :glass_mark_location %><br />
    <%= f.text_field :glass_mark_location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
