<h1>Listing button_shank_materials</h1>

<table>
  <tr>
    <th>Shank material</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @button_shank_materials.each do |button_shank_material| %>
  <tr>
    <td><%= button_shank_material.shank_material %></td>
    <td><%= link_to 'Show', button_shank_material %></td>
    <td><%= link_to 'Edit', edit_button_shank_material_path(button_shank_material) %></td>
    <td><%= link_to 'Destroy', button_shank_material, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Button shank material', new_button_shank_material_path %>
