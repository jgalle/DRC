<%= form_for(@bead_decoration) do |f| %>
  <% if @bead_decoration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bead_decoration.errors.count, "error") %> prohibited this bead_decoration from being saved:</h2>

      <ul>
      <% @bead_decoration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bead_decoration %><br />
    <%= f.text_field :bead_decoration %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
