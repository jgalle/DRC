<h1>Listing ceramic_forms</h1>

<table>
  <tr>
    <th>Ceramic form</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ceramic_forms.each do |ceramic_form| %>
  <tr>
    <td><%= ceramic_form.ceramic_form %></td>
    <td><%= link_to 'Show', ceramic_form %></td>
    <td><%= link_to 'Edit', edit_ceramic_form_path(ceramic_form) %></td>
    <td><%= link_to 'Destroy', ceramic_form, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ceramic form', new_ceramic_form_path %>
