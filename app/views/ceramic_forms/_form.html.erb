<%= form_for(@ceramic_form) do |f| %>
  <% if @ceramic_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ceramic_form.errors.count, "error") %> prohibited this ceramic_form from being saved:</h2>

      <ul>
      <% @ceramic_form.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ceramic_form %><br />
    <%= f.text_field :ceramic_form %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
