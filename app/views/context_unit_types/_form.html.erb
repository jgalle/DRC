<%= form_for(@context_unit_type) do |f| %>
  <% if @context_unit_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@context_unit_type.errors.count, "error") %> prohibited this context_unit_type from being saved:</h2>

      <ul>
      <% @context_unit_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :unit_type %><br />
    <%= f.text_field :unit_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
