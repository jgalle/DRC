<%= form_for(@bone_mark_location) do |f| %>
  <% if @bone_mark_location.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bone_mark_location.errors.count, "error") %> prohibited this bone_mark_location from being saved:</h2>

      <ul>
      <% @bone_mark_location.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bone_mark_location %><br />
    <%= f.text_field :bone_mark_location %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
