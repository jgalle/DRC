<%= form_for(@button_material) do |f| %>
  <% if @button_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@button_material.errors.count, "error") %> prohibited this button_material from being saved:</h2>

      <ul>
      <% @button_material.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :button_material %><br />
    <%= f.text_field :button_material %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
