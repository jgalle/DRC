<%= form_for(@bead_material) do |f| %>
  <% if @bead_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bead_material.errors.count, "error") %> prohibited this bead_material from being saved:</h2>

      <ul>
      <% @bead_material.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bead_material %><br />
    <%= f.text_field :bead_material %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
