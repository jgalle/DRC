<%= form_for(@buckle_part) do |f| %>
  <% if @buckle_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@buckle_part.errors.count, "error") %> prohibited this buckle_part from being saved:</h2>

      <ul>
      <% @buckle_part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :buckle_part %><br />
    <%= f.text_field :buckle_part %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
