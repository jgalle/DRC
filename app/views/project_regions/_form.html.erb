<%= form_for(@project_region) do |f| %>
  <% if @project_region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_region.errors.count, "error") %> prohibited this project_region from being saved:</h2>

      <ul>
      <% @project_region.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :region %><br />
    <%= f.text_field :region %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
