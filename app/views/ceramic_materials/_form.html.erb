<%= form_for(@ceramic_material) do |f| %>
  <% if @ceramic_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ceramic_material.errors.count, "error") %> prohibited this ceramic_material from being saved:</h2>

      <ul>
      <% @ceramic_material.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ceramic_material %><br />
    <%= f.text_field :ceramic_material %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
