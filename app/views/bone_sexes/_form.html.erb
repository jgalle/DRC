<%= form_for(@bone_sex) do |f| %>
  <% if @bone_sex.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bone_sex.errors.count, "error") %> prohibited this bone_sex from being saved:</h2>

      <ul>
      <% @bone_sex.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sex %><br />
    <%= f.text_field :sex %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
