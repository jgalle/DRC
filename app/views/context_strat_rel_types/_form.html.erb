<%= form_for(@context_strat_rel_type) do |f| %>
  <% if @context_strat_rel_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@context_strat_rel_type.errors.count, "error") %> prohibited this context_strat_rel_type from being saved:</h2>

      <ul>
      <% @context_strat_rel_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :strat_rel_type %><br />
    <%= f.text_field :strat_rel_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
