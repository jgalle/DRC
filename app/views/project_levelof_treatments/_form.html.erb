<%= form_for(@project_levelof_treatment) do |f| %>
  <% if @project_levelof_treatment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_levelof_treatment.errors.count, "error") %> prohibited this project_levelof_treatment from being saved:</h2>

      <ul>
      <% @project_levelof_treatment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :levelof_treatment %><br />
    <%= f.text_field :levelof_treatment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
