<%= form_for(@context_screen_size) do |f| %>
  <% if @context_screen_size.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@context_screen_size.errors.count, "error") %> prohibited this context_screen_size from being saved:</h2>

      <ul>
      <% @context_screen_size.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :screen_size %><br />
    <%= f.text_field :screen_size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
