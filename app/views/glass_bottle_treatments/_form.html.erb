<%= form_for(@glass_bottle_treatment) do |f| %>
  <% if @glass_bottle_treatment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@glass_bottle_treatment.errors.count, "error") %> prohibited this glass_bottle_treatment from being saved:</h2>

      <ul>
      <% @glass_bottle_treatment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :glass_bottle_treatment %><br />
    <%= f.text_field :glass_bottle_treatment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
