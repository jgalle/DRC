<%= form_for(@ceramic) do |f| %>
  <% if @ceramic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ceramic.errors.count, "error") %> prohibited this ceramic from being saved:</h2>

      <ul>
      <% @ceramic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :context_sample %><br />
    <%= @ceramic.generate_context_artifact.context_sample_id %>
  </div>
  <div class="field">
    <%= f.label :artifact_id %><br />
    <%= @ceramic.generate_context_artifact.artifact_id %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.text_field :quantity %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
