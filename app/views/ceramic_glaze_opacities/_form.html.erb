<%= form_for(@ceramic_glaze_opacity) do |f| %>
  <% if @ceramic_glaze_opacity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ceramic_glaze_opacity.errors.count, "error") %> prohibited this ceramic_glaze_opacity from being saved:</h2>

      <ul>
      <% @ceramic_glaze_opacity.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ceramic_glaze_opacity %><br />
    <%= f.text_field :ceramic_glaze_opacity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
